#!/usr/bin/env perl

use strict;
use warnings FATAL => 'all';
use utf8;

use lib '..';
use Makefile;

makefile {
   tag => 'ghc',
   version => '8.10.7',

   targets => {
      base => {
         SUFFIX => '',
      },
   },
};
